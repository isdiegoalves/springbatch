spring:
  datasource:
    data-username: sa
    url: jdbc:h2:~/test;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_UPPER=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS public

logging:
  level:
    org.springframework.batch: TRACE
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: DEBUG
    liquibase: TRACE
